# Mapbox Configuration
# REQUIRED: This application requires a Mapbox access token to display the map.
#
# To get your access token:
# 1. Sign up or log in at https://www.mapbox.com/
# 2. Go to https://account.mapbox.com/
# 3. Copy your default public token or create a new one
#
# Then copy this file to .env.local and replace the placeholder below with your actual token:
#   cp .env.example .env.local
#
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_access_token_here

# Site URL Configuration
# REQUIRED: The public URL of your deployed application
# Used for Stripe checkout redirect URLs
NEXT_PUBLIC_SITE_URL=https://www.hydrovacfinder.com

# Stripe Configuration
# REQUIRED: This application requires Stripe for payment processing.
#
# To get your Stripe keys:
# 1. Sign up or log in at https://dashboard.stripe.com/
# 2. Go to Developers > API keys
# 3. Copy your publishable key (starts with pk_) and secret key (starts with sk_)
# 4. For webhooks, go to Developers > Webhooks and create an endpoint
#
# Note: Use test keys (pk_test_... and sk_test_...) for development
#       Use live keys (pk_live_... and sk_live_...) for production
#
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Resend Configuration
# REQUIRED: This application uses Resend for sending emails (referral form).
#
# To get your Resend API key:
# 1. Sign up or log in at https://resend.com/
# 2. Go to API Keys in the dashboard
# 3. Create a new API key with sending permissions
#
# IMPORTANT: You must also verify your domain (hydrovacfinder.com) in Resend:
# 1. Go to Domains in the Resend dashboard
# 2. Add hydrovacfinder.com
# 3. Add the required DNS records (TXT for SPF, CNAME for DKIM) to your domain registrar
#
RESEND_API_KEY=re_your_resend_api_key_here

# Supabase/PostgreSQL Configuration
# REQUIRED: This application uses Supabase as the database backend.
#
# To get your database connection strings:
# 1. Sign up or log in at https://supabase.com/
# 2. Create a new project or select an existing one
# 3. Go to Settings > Database
# 4. Copy the connection strings (Connection Pooler for DATABASE_URL, Direct Connection for DIRECT_URL)
#
# DATABASE_URL is used for pooled connections (recommended for serverless)
# DIRECT_URL is used for migrations and schema operations
#
DATABASE_URL=postgresql://postgres:your_password@db.your_project.supabase.co:5432/postgres
DIRECT_URL=postgresql://postgres:your_password@db.your_project.supabase.co:5432/postgres
